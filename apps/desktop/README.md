# Kissa Desktop

The Electron + React application for Kissa. Run its commands from the repository root when possible:

```bash
npm run dev
npm run format
npm run lint
npm run typecheck
```

Packaging commands remain scoped to this workspace:

```bash
npm run build:win
```
